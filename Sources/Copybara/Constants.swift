enum Constants {
  static let appName = "Copybara"
  static let urlScheme = "copybara"
  static let legacyURLScheme = "dropifier"
  static let supportedURLSchemes = [urlScheme, legacyURLScheme]
  static let bundleIdentifier = "co.showhere.copybara"
  static let maxHistoryItems = 5
}
